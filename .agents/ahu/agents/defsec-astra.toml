schema_version = 1
name = "defsec-astra"
version = "1.0.0"
description = "Reviews and implements defensive programming and focused refactoring"
harness = "codex"
model = "gpt-6-astra"

# Retain the project's existing opt-in to unattended tool use.
permissions = "auto"

[source]
format = "markdown"
path = ".agents/ahu/instructions/defsec-astra.md"
