schema_version = 1
name = "docs-astra"
version = "1.0.0"
description = "Maintains all tracked Markdown, specifications, knowledge formats, and current project context"
harness = "codex"
model = "gpt-6-astra"
permissions = "auto"

[source]
format = "markdown"
path = ".agents/ahu/instructions/docs-astra.md"
