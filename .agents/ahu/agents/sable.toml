schema_version = 1
name = "sable"
version = "1.0.0"
description = "Independent security reviewer (Anthropic)"
harness = "claude-code"
model = "claude-opus-5"

[source]
format = "claude-agent"
path = ".claude/agents/sable.md"
