schema_version = 1
name = "sable"
version = "1.1.0"
description = "Independent security reviewer (Anthropic)"
harness = "claude-code"
model = "claude-opus-5"

# Opt-in: these reviewers run unattended.
permissions = "auto"

[source]
format = "claude-agent"
path = ".claude/agents/sable.md"
