schema_version = 1
name = "orion"
version = "1.1.0"
description = "Independent security reviewer (OpenAI)"
harness = "codex"
model = "gpt-6-astra"

# Opt-in: these reviewers run unattended.
permissions = "auto"

[source]
format = "markdown"
path = ".agents/ahu/instructions/orion.md"
