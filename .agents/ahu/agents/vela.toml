schema_version = 1
name = "vela"
version = "2.1.0"
description = "Independent defensive code-quality reviewer (Google)"
harness = "antigravity"
model = "gemini-3.1-pro-high"

# Opt-in: these reviewers run unattended.
permissions = "auto"

[source]
format = "antigravity-agent"
path = ".agents/agents/vela/agent.md"
